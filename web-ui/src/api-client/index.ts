export type {
  components as hostedComponents,
  operations as hostedOperations,
  paths as hostedPaths,
} from "./generated/hosted-schema";
export { createHostedApiClient, dirtApiBaseUrl } from "./hosted";
