interface:
  display_name: "Simplify"
  short_description: "Parallel cleanup review for changed code"
  default_prompt: "Use $simplify to simplify the current code changes using parallel review agents."
