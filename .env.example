AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# Postgres (see ADR-006). The app assembles DATABASE_URL from the
# DIRT_PG_* components below, but you can set it explicitly to override
# (e.g. pointing at a staging or rehearsal DB).
DIRT_PG_HOST=127.0.0.1
DIRT_PG_PORT=5432
DIRT_PG_USER=dirt
DIRT_PG_PASSWORD=
DIRT_PG_DATABASE=dirt
# DATABASE_URL=postgresql+asyncpg://dirt:PASSWORD@127.0.0.1:5432/dirt

SNAPSHOT_DIR=snapshots
TELEGRAM_BOT_TOKEN=
TELEGRAM_ALLOWED_USER_ID=
SENSOR_INGEST_TOKEN=
PLANT_OTA_PASSWORD=
DEEPGRAM_API_KEY=
ELABS_API_KEY=
ELABS_VOICE_ID=
ANTHROPIC_API_KEY=
KASA_USERNAME=
KASA_PASSWORD=
RUNPOD_API_KEY=
# Container-registry auth ID returned by POST /v1/containerregistryauth.
# Required when the trainer image is on a private registry (e.g. GHCR private).
RUNPOD_GHCR_AUTH_ID=
# ID of a pre-created Network Volume (one-time, via the RunPod console)
# holding the seeded Kaggle datasets under /workspace/input/. Both the
# seeder and the trainer mount this volume.
RUNPOD_NETWORK_VOLUME_ID=
WANDB_API_KEY=
